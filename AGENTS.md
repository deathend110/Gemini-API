# 工作区协作约定

本文件用于约束本工作区内的 agent 协作方式、文档输出方式、编码规范与提交流程。

## 1. 基本原则

- 先理解上下文，再动代码；优先阅读已有代码、文档和约定。
- 优先做最小必要改动，避免无关重构。
- 不得破坏或回滚用户已有的未授权改动。
- 发现风险、歧义或非显然后果时，先说明再继续。
- 结论、状态和验证结果必须基于实际检查，不凭猜测。

## 2. 编码与文件规范

- 本工作区所有新增或修改的文本文件统一使用 UTF-8 编码。
- 默认使用 ASCII 字符；仅在中文文档、既有文件已使用 Unicode、或确有必要时使用非 ASCII 字符。
- 修改代码时优先保持现有项目结构、命名风格与模块边界。
- 搜索文件与文本优先使用 `rg` / `rg --files`。
- 手工编辑文件时使用补丁方式修改，避免用脚本粗暴重写整个文件。

## 3. 文档与沟通规范

- 文档默认使用中文输出，除非任务明确要求英文。
- 对用户的说明、设计文档、实施计划、接入说明、提交说明优先使用中文。
- 文档应强调可执行性，避免空泛表述。

## 4. 开发工作流

- 任何实际编码任务，优先结合 `superpowers` 系列 skill 进行。
- 编码任务默认采用“主 agent + subagent”的协作方式：
  - 主 agent 负责总体方案、任务拆分、集成、验收与最终审核。
  - subagent 负责受控范围内的实现、局部验证和结果汇报。
- 若任务较小且不值得拆分，可以由主 agent 直接完成，但仍应遵守 skill 驱动、先验证后完成的原则。
- 涉及 bug 修复、行为变更、接口变更时，优先补充或更新对应测试。

## 5. 验证与审核

- 声称“完成”“修复”“可用”前，必须先运行与该任务直接相关的验证。
- 主 agent 必须在代码完成后进行一次整体审核，重点检查：
  - 是否满足需求边界
  - 是否影响现有行为
  - 是否存在明显遗漏
  - 是否有必要补充说明或测试
- 若审核未通过，不得进入提交阶段。

## 6. Git 规则

- 审核通过后，默认执行自动 `git commit`。
- 提交说明使用中文，要求简洁、准确、能反映本次改动目的。
- 提交时只包含本次任务相关文件，不捎带无关变更。
- 不得使用破坏性 Git 命令回滚用户改动，如：
  - `git reset --hard`
  - `git checkout -- <file>`
  - 未经允许覆盖或清理用户工作区

## 7. 安全与敏感信息

- 不在代码、文档或提交中泄露密钥、Cookie、令牌或本地凭据。
- `cookies.json`、本地代理配置、个人账号信息等默认视为敏感信息。
- 若任务涉及接入说明，优先提供占位写法或环境变量方式，不直接写死真实凭据。

## 8. 本仓库特别约定

- 面向外部客户端的接口设计优先保持 OpenAI-compatible 风格，便于 AstrBot、Fitness Agent 等项目复用。
- 代理、Cookie、本地路径等本机运行依赖应显式说明，不隐式假设调用方环境。
